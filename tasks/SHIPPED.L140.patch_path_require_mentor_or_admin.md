# L140 – Patch Path requires Mentor or Admin role

**Status**: Shipped  
**Type**: Feature  
**Depends On**: L130  
**Description**: Enforce RBAC on `PATCH /api/path/{PathId}` so only callers whose `roles` include `mentor` or `admin` may update a Path; all other roles are denied with `HTTPForbidden` (`403`). Replace the placeholder `PathService._check_permission` no-op for the `update` operation with a real role check using the shared `Config.ROLE_MENTOR` / `Config.ROLE_ADMIN` constants. There is **no ownership check** — any mentor or admin may patch any Path. Update the OpenAPI `updatePath` operation to document the Mentor-or-Admin requirement (it currently says "requires admin role"). Chained after L130 to serialize `docs/openapi.yaml` edits.

## Context

Always read these files before starting work:

- `../mentorhub/DeveloperEdition/standards/api_standards.md`
- `tasks/README_API.md`
- `README.md`
- `docs/openapi.yaml` (after L130)
- `../mentorhub_api_utils/README.md`

**Prerequisites (local infra & schema source of truth)**

Before starting work, start the backing database and verify the schema source of truth:

- Start the backing database locally: `pipenv run db`
- Verify the schema source of truth:
  `curl http://localhost:8383/api/configurations/json_schema/Path.yaml/latest`

Additional inputs:

- `src/services/path_service.py` — `_check_permission(token, operation)` is a placeholder `pass`; `update_path` calls it with `'update'`. Add the role check here. Import/resolve `Config` (already imported) and read `roles = token.get("roles", []) or []`.
- `src/services/encounter_service.py` — **reference pattern** for the exact "Mentor or Admin" gate: `admin` passes unconditionally, otherwise `mentor` is required, else raise `HTTPForbidden("Mentor or admin role required ...")` (see `_check_permission`, roles/`Config.ROLE_ADMIN`/`Config.ROLE_MENTOR`). Do not edit this file.
- `src/routes/path_routes.py` — **read-only reference**; the PATCH route stays as-is (RBAC lives in the service). Do not edit unless a docstring update is needed (it is listed in Outputs only for an optional endpoint-doc line, no logic change).
- `docs/openapi.yaml` — the `PATCH /api/path/{PathId}` operation (`operationId: updatePath`) description currently reads "requires admin role"; update to "requires mentor or admin role". Ensure a `403` response is present via `#/components/responses/Forbidden`.
- `test/services/test_path_service.py`, `test/routes/test_path_routes.py`, `test/e2e/test_path.py` — add RBAC allow/deny coverage for update.

## Goals

- `PathService._check_permission(token, 'update')` authorizes callers whose `roles` include `Config.ROLE_ADMIN` **or** `Config.ROLE_MENTOR`; every other role (and no-role) is denied with `HTTPForbidden` (`403`). Use the shared config role constants, matching `EncounterService`.
- No ownership check is performed for Path updates — any mentor or admin may patch any Path.
- `update_path` continues to raise `HTTPNotFound` (`404`) for a missing Path and keeps `_validate_update_data` restrictions (`_id`, `created`, `saved`) and the `saved`-breadcrumb behavior; `HTTPForbidden` and `HTTPNotFound` propagate (not wrapped to `500`).
- Behavior of `create_path` and the read paths is unchanged (this task only wires the `update` branch of `_check_permission`; the `read`/`create` branches keep their current behavior).
- `docs/openapi.yaml` `updatePath` description states the Mentor-or-Admin requirement and includes a `403` (`#/components/responses/Forbidden`) response.

## Testing Expectations

Run all commands from the API repository root.

- **Unit tests** — `pipenv run test`. Extend `test/services/test_path_service.py`:
  - `update_path` allowed for `admin`; allowed for `mentor`; denied (`403`) for a role with neither (e.g. `mentee`/`staff`-only) and for a token with no `roles`.
  - Confirm `404` still returned when the target Path does not exist.
  - Route tests (`test/routes/test_path_routes.py`): `PATCH /api/path/<id>` returns `200` for a mentor/admin token and `403` for a non-privileged token.
- **Lint / build** — `pipenv run lint`, `pipenv run build`.
- **Dev / E2E (when infra available)** — `pipenv run db`, `pipenv run dev`, `pipenv run e2e`; extend `test/e2e/test_path.py` with a mentor/admin-allowed PATCH (`200`) and a non-privileged-denied PATCH (`403`) case (`@pytest.mark.e2e`).

## Outputs

Paths are relative to the API repository root.

- `src/services/path_service.py` — real `update` RBAC in `_check_permission` (Mentor or Admin).
- `docs/openapi.yaml` — `updatePath` description + `403` response documenting the Mentor-or-Admin requirement.
- `test/services/test_path_service.py` — update RBAC allow/deny tests.
- `test/routes/test_path_routes.py` — PATCH RBAC route assertions (`200` / `403`).
- `test/e2e/test_path.py` — (optional) mentor/admin-allowed and non-privileged-denied PATCH e2e (`@pytest.mark.e2e`).

The agent must not update files outside this list.

## Execution Notes

### Changes

- `src/services/path_service.py` — `_check_permission(token, operation)` now enforces RBAC for the `update` operation only: `admin` (`Config.ROLE_ADMIN`) passes unconditionally; otherwise `mentor` (`Config.ROLE_MENTOR`) is required, else `HTTPForbidden("Mentor or admin role required to update path documents")`. `read`/`create` remain authenticated-only (no role check). No ownership check. `update_path` already re-raises `(HTTPForbidden, HTTPNotFound)` and preserves `_validate_update_data` + `saved`-breadcrumb behavior.
- `docs/openapi.yaml` — `updatePath` description changed from "requires admin role" to "requires mentor or admin role". The `403` (`#/components/responses/Forbidden`) response was already present and is retained.
- Tests — `test/services/test_path_service.py`: added `test_update_path_allowed_for_mentor`, `test_update_path_denied_for_non_privileged` (403), `test_update_path_denied_for_no_roles` (403), and set `ROLE_ADMIN`/`ROLE_MENTOR` on the existing update tests' config mocks (admin path still allowed, 404 still returned when missing). `test/routes/test_path_routes.py`: added PATCH `200` (authorized) and `403` (service-denied) route assertions. `test/e2e/test_path.py`: added a role-token minter and `@pytest.mark.e2e` PATCH allowed (admin/mentor → 200) and non-privileged denied (mentee → 403) cases.

### Test results

- `pipenv run test` → **196 passed, 32 deselected** (e2e).
- `pipenv run build` → exit 0.
- `pipenv run black` → edited files black-clean.
- E2E / container deferred — container infra (`mh`) not available here; new cases are `@pytest.mark.e2e`. The admin+mentor default persona (`e2e_auth.get_auth_token`) covers the allowed path; the denied path uses a locally minted mentee-only token.

### Note

The Flask route is `/api/path/{PathId}` (singular). The task prose was corrected from `/api/paths/{PathId}` to `/api/path/{PathId}`. RBAC lives in the service, so the route was not modified.